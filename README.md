find the first non repeating character from a string 
//input below str
String str="abachchchz" ;
//output = b

Angular 14

2022

Added:

Angular Material updates
Simplified imports
RxJS 7 support
Angular Ivy enhancements

str.toCharArray().stream


ClassNotFoundException is thrown when we explicitly load a class that is not present in the classpath at runtime.
NoClassDefFoundError occurs when a class was available during compilation but cannot be found by the JVM at runtime, usually due to missing dependencies or classloader issues.



Supplier → supplies data
Consumer → consumes data
Predicate → returns boolean
Function → transforms data


PUT → full update - Idempotent.
PATCH → partial update

POST - to create a resource - Non Idempotent.


Kafka Streams Repartitioning

Triggered when key changes.
Uses internal repartition topics.


Mock → no real behavior
Spy → partial real behavior


Use mockito-inline and MockedStatic.


@Mock → creates fake dependency
@InjectMocks → injects mocks into class


React → UI library, virtual DOM
AngularJS → deprecated MVC framework

Virtual DOM = performance optimization
✅ Reduces direct DOM manipulation
✅ Enables fast, scalable UI updates
✅ Core reason React performs well







